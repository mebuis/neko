# README

## Usage

```bash
python3 scan.py [-h] [-f FILE | -d DIRECTORY] [-o OUTPUT] [-s SUFFIX]

eg. python3 scan.py -f SAMPLE.rtf -o output -s output.json

The output file will be stored into the "output" directory, with a name "[SAMPLE.rtf]-output.json".
```
